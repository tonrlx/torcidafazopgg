/**
 * Core Module - Essential application logic
 * This module contains the main application structure and routing
 */

export { default as App } from '../../App';
export { useTabNavigation } from '../../hooks/useTabNavigation';
export { useMenu } from '../../hooks/useMenu';
export { TABS, ANIMATION_DURATION } from '../../constants';